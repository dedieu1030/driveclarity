{
  "name": "drive-access-viewer-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions — Stripe webhooks + subscription API",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^15.7.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.3.0"
  }
}
